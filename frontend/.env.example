# Copy to .env. The frontend holds no secrets — this is just the API location.
#   cp .env.example .env
#
# Must match where uvicorn is serving the backend. Note the backend's own
# CORS_ORIGINS must in turn allowlist this app's origin (http://localhost:5173).
VITE_API_BASE_URL=http://127.0.0.1:8000
